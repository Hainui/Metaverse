<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metaverse.user.db.mapper.MetaverseUserFriendQuestionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.metaverse.user.db.entity.MetaverseUserFriendQuestionDO">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="question" property="question" />
        <result column="correct_answer" property="correctAnswer" />
        <result column="enabled" property="enabled" />
        <result column="saved_at" property="savedAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, question, correct_answer, enabled, saved_at
    </sql>

</mapper>
