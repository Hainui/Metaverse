<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metaverse.user.db.mapper.MetaverseUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.metaverse.user.db.entity.MetaverseUserDO">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="avatar_file_id" property="avatarFileId" />
        <result column="gender" property="gender" />
        <result column="birth_time" property="birthTime" />
        <result column="saved_at" property="savedAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="update_by" property="updateBy" />
        <result column="region_id" property="regionId" />
        <result column="version" property="version" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password, email, avatar_file_id, gender, birth_time, saved_at, updated_at, update_by, region_id, version
    </sql>

</mapper>
