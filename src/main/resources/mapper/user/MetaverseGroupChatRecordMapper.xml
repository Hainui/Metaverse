<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metaverse.user.db.mapper.MetaverseGroupChatRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.metaverse.user.db.entity.MetaverseGroupChatRecordDO">
        <id column="id" property="id" />
        <result column="group_id" property="groupId" />
        <result column="sender_id" property="senderId" />
        <result column="message_type" property="messageType" />
        <result column="saved_at" property="savedAt" />
        <result column="content" property="content" />
        <result column="media_url" property="mediaUrl" />
        <result column="is_withdrawn" property="withdrawn" />
        <result column="timestamp" property="timestamp" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, group_id, sender_id, message_type, saved_at, content, media_url, is_withdrawn, timestamp
    </sql>

</mapper>
