<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metaverse.user.db.mapper.MetaverseChatRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.metaverse.user.db.entity.MetaverseChatRecordDO">
        <result column="sender_id" property="senderId" />
        <result column="receiver_id" property="receiverId" />
        <result column="message_type" property="messageType" />
        <result column="content" property="content" />
        <result column="file_id" property="fileId" />
        <result column="saved_at" property="savedAt" />
        <result column="timestamp" property="timestamp" />
        <result column="is_withdrawn" property="withdrawn" />
        <result column="withdrawn_time" property="withdrawnTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        sender_id, receiver_id, message_type, content, file_id, saved_at, timestamp, is_withdrawn, withdrawn_time
    </sql>

</mapper>
